# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.
# NOTE: The compiled site (index.html, /static, etc.) lives at the repo root and
# IS committed — GitHub Pages serves it from here. Do not ignore those.

# OS / editor junk
.DS_Store
*.log

# Stray dependencies (real ones live in /app/node_modules)
/node_modules

# # Local source material (CV PDF + photos used to build the site)
# /contex
